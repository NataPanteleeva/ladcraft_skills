/**
 * @deprecated Import from `../transfer/context-sync` instead.
 * Re-exports for backward compatibility during migration.
 */
export {
  ensureDocumentContext,
  computeDocumentHash,
  isDocumentDirty,
  isContextBoundToDocument,
  scheduleContextSync,
  type DocumentContextState,
  type EnsureContextResult,
  type EnsureContextOptions,
} from "../transfer/context-sync";
